#include "main.h"
/**
 * _abs - this function calculates the absolute value of an integer
 * @n: the integer to be checked
 * Return: this will return the absolute value of int
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
